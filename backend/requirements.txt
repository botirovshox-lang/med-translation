# Medical CAT Translator — FastAPI backend
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
python-multipart>=0.0.6
orjson>=3.9   # тяжёлые выдачи (/api/seed, проект) готовыми байтами; без него — json, медленнее

# Optional: import existing modules if available (not required)
python-docx>=1.1   # импорт и экспорт .docx: без него upload_project отвечает 500
# openai>=1.0.0
anthropic>=1.0   # второй поставщик моделей (Claude); без ключа ANTHROPIC_API_KEY не зовётся
# google-cloud-translate>=3.8.0

# ── Текст, впечатанный в картинки (OCR) ─────────────────────────────
# Геометрию строк даёт локальный детектор RapidOCR, текст читает зрячая
# модель: распознавалка, идущая с детектором, знает китайский и английский,
# а кириллицу возвращает латинской абракадаброй.
#
# ВАЖНО, как ставить на сервер:
#     pip install -r requirements.txt
#     pip install --no-deps rapidocr-onnxruntime
# Именно --no-deps: своей зависимостью rapidocr тянет opencv-python, а тот
# требует libGL.so.1, которого на сервере без графики нет — импорт cv2 падает.
# Тот же модуль даёт headless-сборка, она и стоит в списке ниже.
opencv-python-headless>=4.9
onnxruntime>=1.17
pyclipper>=1.3
Shapely>=2.0
PyYAML>=6.0
Pillow>=10.0
numpy>=1.24
psycopg[binary]>=3.1   # хранилище PostgreSQL (DATABASE_URL); без него — файл

# ── Объём файла в страницах (backend/textcount.py) ──────────────────
# Смету по PDF без этого посчитать нечем: эндпоинт честно отвечает 503
# «нет модуля», а не считает файл нулём. Чистый Python, без системных
# библиотек: из PDF берётся текстовый слой, а у СКАНА — вложенная картинка
# страницы как она лежит (`pdf_page_images`).
pypdf>=4.0
# Рендер страницы целиком (`pdf_render_pages`). Понадобился на многослойном
# скане (обложка «Пчелиной аптеки»): страница там — фон + маска с текстом,
# и крупнейшая вложенная картинка несёт только заголовок. Колёсико со своим
# pdfium, системных библиотек не тянет. Нет модуля — берётся вложенная
# картинка, как раньше; страницы с ненадёжным текстовым слоем без
# страничной картинки остаются текстом.
pypdfium2>=4.30

# ── Выгрузка PDF «как в оригинале» (backend/layout_pdf.py) ──────────
# Перевод встаёт НА МЕСТО оригинала прямо в исходном PDF: страницы книги
# остаются как были (рисунки, колонки, колонтитулы), а поверх каждого
# абзаца кладётся закраска цветом его же бумаги и переведённый текст.
# reportlab — чистый Python, системных библиотек не тянет; из него нужен
# только слой с текстом (страницы склеивает pypdf).
#
# ШРИФТ БЕРЁТСЯ ИЗ СИСТЕМЫ и обязан иметь письменность целевого языка:
# на сервере это /usr/share/fonts/truetype/dejavu (ставится с LibreOffice).
# Нет шрифта — выгрузка отвечает «нет шрифта», а не печатает квадраты.
# Свой путь — LAYOUT_FONT / LAYOUT_FONT_BOLD в окружении.
reportlab>=4.0

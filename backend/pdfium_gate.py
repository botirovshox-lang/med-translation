"""Один замок на ВСЮ работу с pdfium (pypdfium2) в процессе.

Зачем: pdfium не потокобезопасен, а зовём мы его из потоков — обработчик
HTTP идёт в threadpool, прогон раздаёт работу своим потокам. Падает при
этом не наш код, а нативная библиотека, и питоновского исключения тут
не бывает: вместе с ней умирает ВЕСЬ процесс, то есть и чужие запросы,
и идущий прогон, и уже начатая отдача файла.

Так и вышло на боевом сервере 20.09: два падения подряд посреди выгрузки
книги «как в оригинале» — `general protection fault` и `int3 in libpdfium.so`,
— и сервис уходил в перезапуск ровно в ту секунду, когда человек забирал
готовый файл. Снаружи это выглядело как «система не даёт выгрузить файл».

Второй причиной было то, что `layout_pdf._Sampler` НЕ ЗАКРЫВАЛ документ:
его прибирал сборщик мусора, когда придётся и в каком придётся потоке, —
отсюда падение через сорок секунд после последнего запроса, когда в pdfium
уже никто не заходил. Поэтому правило двойное: документ закрывается ЯВНО,
а всё, что трогает pdfium (открытие, отрисовка, закрытие), идёт под этим
замком.

RLock, а не Lock: закрытие документа законно случается внутри уже взятого
замка."""

import threading

LOCK = threading.RLock()

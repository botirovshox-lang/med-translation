# Промпт для Claude Design: Локализация + Tooltips

Скопируй текст ниже в Claude Design.

---

```
Доработай существующий дизайн Medical CAT Translator v5.5.
Сохрани всю текущую структуру, цвета, темы и layout.
Задачи: (1) полная локализация на русский, (2) добавление info-tooltips 
с иконками ⓘ возле каждого технического термина.

═══════════════════════════════════════════════════════════════
ОБЩАЯ ЛОГИКА TOOLTIPS
═══════════════════════════════════════════════════════════════

Рядом с каждым техническим термином/функцией добавь иконку ⓘ 
(lucide-react: "info", 14px, muted color).

При hover/click → показывается popover/tooltip:
- max-width: 320px
- padding: 12px 16px
- background: surface-elevated (контраст с фоном)
- border: 1px subtle
- border-radius: 8px
- shadow: medium
- Заголовок (bold, 14px)
- Описание (regular, 13px, line-height 1.5)
- При необходимости: код-плашка с английским термином

═══════════════════════════════════════════════════════════════
СТРАНИЦА: PREFLIGHT / АНАЛИЗ
═══════════════════════════════════════════════════════════════

Текущие английские значения → перевод + tooltip:

┌─── HEADER ─────────────────────────────────────────
"Preflight / Cost + Safety Planner"
  → "🔍 Анализ проекта / Стоимость + безопасность" ⓘ
  
  Tooltip: "Локальный анализ всех сегментов БЕЗ вызовов API.
  Определяет: маршрут перевода, риски, стоимость, дубликаты,
  возможности оптимизации. Безопасно запускать любое количество раз."

┌─── КНОПКИ ────────────────────────────────────────
"Analyze Only" → "🔍 Запустить анализ" ⓘ
  Tooltip: "Анализ только локально, без вызовов API.
  Результат сохраняется в базу — можно использовать
  для планирования перевода."

┌─── STATISTICS ────────────────────────────────────
"Statistics" → "📊 Статистика"

"Total Segments" → "Всего сегментов" ⓘ
  Tooltip: "Общее количество сегментов в проекте после 
  импорта DOCX и сегментации."

"Unique (normalized)" → "Уникальных (нормализованных)" ⓘ
  Tooltip: "Количество сегментов с уникальным текстом
  после нормализации (приведение к нижнему регистру,
  удаление пунктуации, тримминг пробелов). Меньше total —
  значит есть дубликаты."

"Duplicate Groups" → "Групп дубликатов" ⓘ
  Tooltip: "Количество групп, где 2+ сегментов имеют
  одинаковый нормализованный текст. Перевод одного
  representative-сегмента в группе автоматически копируется
  на все остальные → экономия токенов."

"Exact TM (99%+)" → "Точных совпадений TM (99%+)" ⓘ
  Tooltip: "Сегменты с совпадением ≥99% в Translation Memory.
  Перевод подтягивается из TM без вызова GPT → 
  стоимость = $0."

"Glossary Coverage" → "Покрытие глоссарием" ⓘ
  Tooltip: "Процент сегментов, в которых найден хотя бы один
  термин из утверждённого медицинского глоссария.
  Высокое покрытие → лучше предсказуемость терминологии."

"Analysis Time" → "Время анализа (сек)" ⓘ
  Tooltip: "Время локального анализа в секундах.
  Цель: < 120с для 2828 сегментов."

┌─── ROUTING SUMMARY ───────────────────────────────
"Routing Summary" → "📍 Маршруты обработки" ⓘ
  Tooltip заголовка: "Распределение сегментов по маршрутам
  перевода. Каждый маршрут оптимизирован под тип контента
  для минимизации стоимости."

Колонки таблицы:
  "Route" → "Маршрут"
  "Segments" → "Сегментов"  
  "Percentage" → "Доля"

ЗНАЧЕНИЯ маршрутов (это ключевое — переводим + tooltip 
для КАЖДОГО):

┌─────────────────────────────────────────────────────┐
│ Английский код           │ Русский лейбл          │
├─────────────────────────────────────────────────────┤
│ EXACT_TM                 │ 🟢 Точное TM           │
│ DUPLICATE_REPRESENTATIVE │ 🔵 Представитель дубл. │
│ DUPLICATE_PROPAGATION_   │ 🟣 Копия из дубликата  │
│   PENDING                │                        │
│ GOOGLE_SAFE              │ 🟡 Google безопасно    │
│ GPT_REQUIRED             │ ⚪ Требуется GPT        │
│ GPT_WITH_GLOSSARY_       │ 🟠 GPT + Глоссарий     │
│   REQUIRED               │                        │
│ HUMAN_REVIEW_REQUIRED    │ 🔴 Ручной перевод      │
└─────────────────────────────────────────────────────┘

Tooltips (ТОЧНЫЕ описания из кода safety_policy.py):

EXACT_TM ⓘ
  "Точное совпадение в Translation Memory (99%+) — 
  использовать существующий перевод. Стоимость: $0.
  Не требует вызова API."

DUPLICATE_REPRESENTATIVE ⓘ
  "Первый сегмент в группе дубликатов — перевести один раз
  через GPT. Остальные сегменты группы получат копию.
  Стоимость: только за representative."

DUPLICATE_PROPAGATION_PENDING ⓘ
  "Дубликат другого сегмента — скопировать перевод от 
  representative после его подтверждения. Стоимость: $0.
  Не требует вызова API."

GOOGLE_SAFE ⓘ
  "Низкий риск, нет анатомии → использовать Google Translate
  (бесплатный tier до 500K символов/месяц). Подходит для
  простых сегментов без медицинской критичности."

GPT_REQUIRED ⓘ
  "Требуется OpenAI GPT для перевода и QA.
  Стандартный маршрут для большинства медицинских сегментов."

GPT_WITH_GLOSSARY_REQUIRED ⓘ
  "Требуется OpenAI GPT с инъекцией глоссария в промпт.
  Применяется для сегментов с большим количеством медицинских
  терминов (3+) или с риском HIGH."

HUMAN_REVIEW_REQUIRED ⓘ
  "Критичный или высокий риск без глоссария — требуется
  человеческий перевод/review. API не вызывается до решения
  переводчика."

┌─── RISK SUMMARY ──────────────────────────────────
"Risk Summary" → "⚠️ Сводка по рискам" ⓘ
  Tooltip заголовка: "Уровень риска рассчитывается локально
  по эвристикам: семантическая плотность, медицинские термины,
  числа/дозировки, анатомия. Влияет на выбор маршрута и QA."

Колонки:
  "Risk Level" → "Уровень риска"
  "Segments" → "Сегментов"
  "Percentage" → "Доля"

Значения с tooltips:

CRITICAL ⓘ — 🔴 Критический (75-100)
  "Скор риска 75-100. Содержит критичную медицинскую
  информацию: дозировки, противопоказания, реанимация.
  → Маршрут: HUMAN_REVIEW_REQUIRED. QA политика: strict."

HIGH ⓘ — 🟠 Высокий (50-74)
  "Скор риска 50-74. Сложная медицинская терминология,
  анатомия, диагнозы.
  → Маршрут: GPT_WITH_GLOSSARY_REQUIRED. QA политика: strict."

MEDIUM ⓘ — 🟡 Средний (25-49)
  "Скор риска 25-49. Стандартный медицинский контент,
  отчёты, описания.
  → Маршрут: GPT_REQUIRED. QA политика: manual."

LOW ⓘ — 🟢 Низкий (0-24)
  "Скор риска 0-24. Метаданные, простые предложения,
  заголовки.
  → Маршрут: GOOGLE_SAFE. QA политика: auto_pass."

┌─── COST ESTIMATE ─────────────────────────────────
"Cost Estimate (USD)" → "💰 Оценка стоимости (USD)" ⓘ
  Tooltip: "Прогноз стоимости API-вызовов на основе токенов.
  Точность ±15% — реальная стоимость может отличаться."

"Baseline Cost (all via GPT)" → "Базовая стоимость (всё через GPT)" ⓘ
  Tooltip: "Сколько стоило бы перевести ВСЕ сегменты через
  GPT-4 без оптимизации (translate + QA + back-check + safety)."

"Optimized Cost (with routing)" → "Оптимизированная стоимость" ⓘ
  Tooltip: "Прогноз с применением маршрутизации: дубликаты,
  Google для простых, TM для совпадений, GPT только для сложных."

"Potential Savings" → "Потенциальная экономия" ⓘ
  Tooltip: "Сколько сэкономите при использовании оптимизированного
  маршрута вместо базового. Формула: Baseline − Optimized."

┌─── COST COMPONENTS BREAKDOWN ─────────────────────
"Cost Components Breakdown" → "📊 Разбивка по компонентам" ⓘ
  Tooltip: "Стоимость по этапам обработки. Видно где можно
  сэкономить больше всего (часто это Back-check и Safety)."

Колонки и термины:
  "Component" → "Компонент"
  "Baseline ($)" → "Базовая ($)"
  "Optimized ($)" → "Оптимизированная ($)"
  "Savings ($)" → "Экономия ($)"

  "Translation" → "Перевод" ⓘ
    "Стоимость перевода через GPT-4 
    (input + output tokens × цена)."
  "QA" → "Проверка качества" ⓘ
    "Автоматическая проверка перевода через GPT-4:
    8 локальных проверок + консистентность + численные данные."
  "Back-check" → "Обратная проверка" ⓘ
    "Обратный перевод (target→source) через GPT-4 для проверки
    смысловой эквивалентности. Запускается только для HIGH/CRITICAL."
  "Safety" → "Проверка безопасности" ⓘ
    "Финальная проверка на запрещённые термины, ошибки
    в дозировках, медицинскую корректность."
  "Google" → "Google Translate" ⓘ
    "Стоимость Google Translate (бесплатно до 500K символов/мес,
    далее $20 за 1M символов)."

┌─── ROUTE COST BREAKDOWN ──────────────────────────
"Route Cost Breakdown" → "📍 Стоимость по маршрутам" ⓘ
  Tooltip: "Сколько стоит каждый маршрут в отдельности.
  Помогает понять основную статью расходов."

Колонки:
  "Route" → "Маршрут"
  "Segments" → "Сегментов"
  "Tokens" → "Токены"
  "Baseline ($)" → "Базовая ($)"
  "Optimized ($)" → "Оптимизированная ($)"
  "Savings ($)" → "Экономия ($)"

┌─── ZERO-TOKEN OPTIMIZATION ───────────────────────
"Zero-Token Optimization" → "⚡ Оптимизация без токенов" ⓘ
  Tooltip: "Действия, которые НЕ требуют вызовов API:
  заполнение из TM (точные совпадения) и копирование
  переводов между дубликатами. Стоимость: $0."

"Reduce API calls before translation:" → 
"Сократите количество API-вызовов перед переводом:"

"Exact TM: Fill from trusted Translation Memory (0 tokens)" →
"Точное TM: заполнить из доверенной памяти переводов (0 токенов)" ⓘ
  Tooltip: "Найти сегменты с совпадением ≥99% в TM и подставить
  существующий перевод. API не вызывается."

"Duplicates: Copy confirmed translations to duplicates (0 tokens)" →
"Дубликаты: скопировать подтверждённые переводы дубликатам (0 токенов)" ⓘ
  Tooltip: "После подтверждения representative-сегмента, его
  перевод автоматически копируется всем дубликатам в группе."

КНОПКИ:
  "Apply Exact TM" → "💾 Применить точное TM" ⓘ
    Tooltip: "Заполнить сегменты с TM ≥99%. Безопасно: 
    переводы можно изменить вручную после."
  
  "Prepare Representatives" → "📌 Подготовить representatives" ⓘ
    Tooltip: "Отметить первый сегмент каждой группы дубликатов
    как 'representative'. Эти сегменты будут переводиться,
    остальные получат копию."
  
  "Propagate Duplicates" → "🔁 Распространить дубликаты" ⓘ
    Tooltip: "Скопировать переводы representatives на все
    дубликаты в их группах. Запускать после подтверждения
    representatives."
  
  "Show Duplicate Groups" → "📋 Показать группы дубликатов" ⓘ
    Tooltip: "Открыть список всех групп дубликатов с возможностью
    просмотра сегментов в каждой группе."

┌─── RECOMMENDED BATCH ORDER ───────────────────────
"Recommended Batch Order" → "📋 Рекомендуемый порядок обработки" ⓘ
  Tooltip: "Оптимальный порядок перевода сегментов:
  сначала representatives дубликатов, затем GPT_REQUIRED,
  затем HUMAN_REVIEW. Помогает максимизировать использование
  кэша и снизить затраты."

"Process segments in this order to optimize costs:" →
"Обработайте сегменты в этом порядке для оптимизации затрат:"

"Top 10 segment IDs:" → "Топ-10 ID сегментов:"

"(Showing first 10 of 50 recommended)" →
"(Показаны первые 10 из 50 рекомендуемых)"

═══════════════════════════════════════════════════════════════
СТРАНИЦА: SEGMENT EDITOR / РЕДАКТОР
═══════════════════════════════════════════════════════════════

(уже в основном русифицирован, добавь tooltips:)

Статусы:
  "new" → "Новый" ⓘ
    "Сегмент не переведён. Никаких операций не выполнялось."
  "translated" → "Переведён" ⓘ
    "Перевод получен (Google/GPT/вручную). QA ещё не запускалось."
  "qa_done" → "QA пройдено" ⓘ
    "Автоматическая проверка качества выполнена. Можно подтверждать."
  "back_checked" → "Back-check пройдено" ⓘ
    "Обратный перевод проверил смысловую эквивалентность."
  "confirmed" → "Подтверждён" ⓘ
    "Финальный статус. Добавлен в TM. Будет в экспорте."
  "needs_review" → "Требует review" ⓘ
    "Обнаружены проблемы — необходим человеческий просмотр."
  "failed" → "Ошибка" ⓘ
    "Перевод/QA завершились с ошибкой. См. логи."

Кнопки пакетных операций:
  "Preview Google Batch" → "👁 Превью Google batch" ⓘ
    "Показать сколько сегментов попадут в Google-перевод,
    оценочную стоимость и примеры. Без фактического вызова API."
  
  "Run Google Batch" → "▶ Запустить Google batch" ⓘ
    "Перевести все GOOGLE_SAFE сегменты через Google Translate.
    Результат сохраняется как 'google_draft' (не подтверждён)."
  
  "Preview GPT Batch" → "📊 Превью GPT batch" ⓘ
    "Показать сегменты для GPT-перевода, оценку токенов,
    стоимость и глубину QA. Без вызова API."
  
  "Run GPT Batch" → "🤖 Запустить GPT batch" ⓘ
    "Перевод через OpenAI GPT с QA и применением глоссария.
    Результат: status='translated', provider='openai'."

═══════════════════════════════════════════════════════════════
СТРАНИЦА: QA DASHBOARD
═══════════════════════════════════════════════════════════════

"QA Dashboard" → "✔️ Контроль качества" ⓘ
  "6-этапный pipeline QA: локальные проверки (8) → 
  консистентность → адаптивное планирование → проверка чисел →
  back-check → финальное решение."

QA политики:
  "auto_pass" → "Авто-подтверждение" ⓘ
    "Только локальные проверки. Без вызовов API. 
    Применяется для LOW риска."
  "manual" → "Ручная проверка" ⓘ
    "Локальные + GPT QA, без back-check. Для MEDIUM риска."
  "strict" → "Строгая проверка" ⓘ
    "Полный pipeline: всё включая back-check + safety review.
    Для HIGH/CRITICAL риска."

Категории проблем:
  "Formatting Issues" → "Проблемы форматирования" ⓘ
    "Несоответствие регистра, пунктуации, пробелов
    между оригиналом и переводом."
  "Numerical Errors" → "Численные ошибки" ⓘ
    "Несовпадение чисел, дозировок, единиц измерения
    между оригиналом и переводом (КРИТИЧНО для медицины)."
  "Terminology Warnings" → "Предупреждения по терминологии" ⓘ
    "Использованы запрещённые термины или термины не из
    утверждённого глоссария."
  "Length Issues" → "Проблемы длины" ⓘ
    "Перевод существенно длиннее/короче оригинала (>2×).
    Может указывать на пропуск или добавление информации."

═══════════════════════════════════════════════════════════════
СТРАНИЦА: GLOSSARY / ГЛОССАРИЙ
═══════════════════════════════════════════════════════════════

"Glossary" → "📚 Глоссарий" ⓘ
  "База утверждённых медицинских терминов с переводами.
  Используется для инъекции в GPT-промпт и проверки
  консистентности в QA."

Колонки таблицы:
  "Source term" → "Исходный термин"
  "Target term" → "Перевод"
  "Category" → "Категория" ⓘ
    "Anatomy (анатомия), Dosage (дозировки),
    Disease (заболевания), Device (медтехника),
    Procedure (процедуры) и др."
  "Frequency" → "Частота" ⓘ
    "Сколько раз термин встречался в проектах."
  "Confidence" → "Уверенность" ⓘ
    "High — проверен экспертом, Medium — авто-извлечён,
    Low — требует проверки."

═══════════════════════════════════════════════════════════════
СТРАНИЦА: TM / TRANSLATION MEMORY
═══════════════════════════════════════════════════════════════

"TM" / "Translation Memory" → "🔁 Память переводов (TM)" ⓘ
  "База подтверждённых пар (оригинал → перевод).
  Используется для поиска точных и нечётких совпадений
  в новых проектах. Экономит токены."

Метрики:
  "TM Match Score" → "TM-совпадение (%)" ⓘ
    "Процент схожести нового сегмента с найденным в TM.
    100% = идеальное совпадение, ≥99% = маршрут EXACT_TM."
  "Verified" → "✅ Проверено" ⓘ
    "Перевод подтверждён человеком."
  "Draft" → "⚠️ Черновик" ⓘ
    "Перевод авто-сгенерирован, нужна проверка."

═══════════════════════════════════════════════════════════════
СТРАНИЦА: STATS / СТАТИСТИКА
═══════════════════════════════════════════════════════════════

"Statistics" → "📊 Статистика проекта"

Метрики:
  "Velocity" → "Скорость" ⓘ
    "Сегментов переведено в час. Помогает планировать сроки."
  "Throughput" → "Производительность" ⓘ
    "Слов обработано в день командой."
  "Quality Score" → "Оценка качества" ⓘ
    "% сегментов, прошедших QA без замечаний."

═══════════════════════════════════════════════════════════════
СТРАНИЦА: BACKLOG / БЭКЛОГ
═══════════════════════════════════════════════════════════════

"Backlog" → "📋 Бэклог задач" ⓘ
  "Список сегментов, требующих внимания: HIGH/CRITICAL риск,
  failed QA, неконсистентная терминология. Приоритизация:
  CRITICAL > численные > семантические > глоссарий."

═══════════════════════════════════════════════════════════════
ОБЩИЕ ПРИНЦИПЫ ЛОКАЛИЗАЦИИ
═══════════════════════════════════════════════════════════════

✅ ПЕРЕВОДИТЬ:
- Все заголовки секций
- Все кнопки
- Все колонки таблиц
- Все статусы
- Все метки полей
- Все короткие фразы UI

❌ НЕ ПЕРЕВОДИТЬ:
- Коды маршрутов в БД (EXACT_TM, GPT_REQUIRED и т.д.)
  → показывать русский лейбл + код в моноширинном шрифте
  как secondary text: "Точное TM (EXACT_TM)"
- Технические идентификаторы (ID сегментов, hash)
- Названия моделей: GPT-4o, Claude, Google Translate
- API термины: tokens, prompt, completion

═══════════════════════════════════════════════════════════════
СТИЛЬ ИКОНОК ⓘ
═══════════════════════════════════════════════════════════════

- Размер: 14px
- Цвет: muted-foreground (60% opacity)
- Hover: foreground (100%)
- Cursor: help
- Размещение: после текста, отступ 4px
- Активация: hover на desktop, tap на mobile
- Не показывать ⓘ если описания нет

═══════════════════════════════════════════════════════════════
СТИЛЬ TOOLTIP
═══════════════════════════════════════════════════════════════

Light theme:
- bg: #ffffff
- border: 1px solid #e5e7eb
- shadow: 0 10px 25px rgba(0,0,0,0.1)
- text: #1a1a1a
- title: 14px bold
- body: 13px regular, line-height 1.5

Dark theme:
- bg: #2a2a2a
- border: 1px solid #4a4a4a
- shadow: 0 10px 25px rgba(0,0,0,0.4)
- text: #e0e0e0
- title: 14px bold, color: white
- body: 13px regular, line-height 1.5

Анимация: fade-in 150ms ease-out
Задержка показа: 300ms (чтобы не мешать беглому просмотру)

═══════════════════════════════════════════════════════════════
ЧТО НЕ ТРОГАТЬ
═══════════════════════════════════════════════════════════════

✓ Общую структуру (header, tabs, sidebar)
✓ Цветовую систему (light/dark)
✓ Sticky-навигацию, фильтры
✓ Логику кнопок и форм
✓ Шрифты, отступы, grid
✓ Все ранее одобренные элементы

═══════════════════════════════════════════════════════════════
DELIVERABLES
═══════════════════════════════════════════════════════════════

1. Все 9 страниц с локализованным контентом
2. Tooltips для всех технических терминов
3. Mockup со всеми типами tooltips видимыми (3-4 примера)
4. Sample state: открыт Preflight с полностью русским контентом
   и tooltips на маршрутах + рисках
5. Both light + dark themes
6. Сохранить английские коды как secondary text 
   для совместимости с разработчиками
```

---

## 🎯 Что меняется:

| Было (EN) | Станет (RU) | + Tooltip |
|-----------|-------------|-----------|
| EXACT_TM | 🟢 Точное TM | ⓘ Описание из safety_policy.py |
| GPT_REQUIRED | ⚪ Требуется GPT | ⓘ Полное описание |
| HUMAN_REVIEW_REQUIRED | 🔴 Ручной перевод | ⓘ Когда применяется |
| Routing Summary | 📍 Маршруты обработки | ⓘ Что это и зачем |
| Risk Summary | ⚠️ Сводка по рискам | ⓘ Как рассчитывается |
| Cost Estimate | 💰 Оценка стоимости | ⓘ Точность ±15% |
| Zero-Token Optimization | ⚡ Без токенов | ⓘ Как работает |
| QA: auto_pass / manual / strict | Авто/Ручная/Строгая | ⓘ Когда каждая |

И так для **всех 9 страниц**. Промпт содержит **точные описания из кода** (`safety_policy.py`).

Скопируй промпт выше → вставь в Claude Design → получишь полностью локализованную версию с обучающими подсказками! 🎨